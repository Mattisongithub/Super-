using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SuperStrength : MonoBehaviour {

	private Dictionary<string, int> normalStrength = new Dictionary<string, int> ();

	public void use ()
	{
		//TODO !!!!!!!!!!!!!!!!!!!!!!!!!!!
		/*PlayerStats ps = new PlayerStats ();
		if (ps.strength.TryGetValue (gameObject.name) == 30) {
			ps.strength.Remove (gameObject.name);
			ps.strength. (gameObject.name, normalStrength.val (gameObject.name));
		} else {
			ps.strength.Remove (gameObject.name);
			ps.strength.Add (gameObject.name, 30);
		}*/
	}

}
